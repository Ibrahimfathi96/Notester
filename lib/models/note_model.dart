import 'package:flutter/animation.dart';

class Note {
  String title;
  String description;
  String dateTime;
  Color color;

  Note(
      {required this.title,
      required this.description,
      required this.dateTime,
      required this.color});
}
